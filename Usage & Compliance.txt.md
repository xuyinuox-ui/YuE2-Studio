# YuE2 Studio for macOS｜使用说明、发布说明与合规提示

> 适用版本：1.2.5（Apple Silicon）  
> 更新日期：2026-09-18  
> 本文提供产品与许可信息，不构成法律意见。

## 联系与制作信息

- 联系邮箱：<xuyinuox@163.com>
- 本 App 由 **GPT-5.6 Sol** 完成制作
- 问题反馈时，请附上 macOS 版本、Mac 芯片型号、App 版本及从 `[installer]` 开始的完整日志。

## 1. 产品简介

YuE2 Studio 是一款本地运行的 macOS AI 音乐工作台，提供：

- 音频转 ABC：使用 SheetSage2 与 MERT 提取节拍、调性、结构、人声及器乐旋律；
- 音乐生成：使用 YuE2-3B 与 YuE2-Vae，默认通过 MLX 在 Apple Silicon 上运行；
- 一键安装：首次启动自动安装独立 Python 环境、依赖与完整模型；
- 国内镜像与断点续传：自动测速、分段下载、保留未完成分片；
- 自检修复：按最终文件和精确大小检查模型，缺哪个只补哪个；
- 任务控制：转写和生成均可中断；
- 本地历史、日志、播放与干净卸载。

## 2. 系统要求

| 项目 | 要求 |
|---|---|
| 处理器 | Apple Silicon（M1 或更新）；不支持 Intel Mac |
| 系统 | macOS 14.0 或更新版本 |
| 内存 | 建议 16 GB；更长歌曲建议 24 GB 或以上 |
| 可用空间 | 首次安装建议预留至少 15 GB |
| 网络 | 首次安装必须联网；安装完成后核心转写与生成可本地运行 |

## 3. 安装与首次启动

1. 将 `YuE2 Studio.app` 放入“应用程序”文件夹。
2. 双击启动。首次启动会检查环境并列出待安装内容。
3. 点击安装后保持网络与电源稳定。下载中断不会丢失已完成分段，下次启动会续传。
4. 安装器会校验 YuE2-3B、YuE2-Vae、SheetSage2 与 MERT 的最终权重；仅有目录或残留分片不会被判定为完成。
5. 出现“完成”并显示“worker 就绪”后即可使用。

运行数据按当前 macOS 用户动态保存到：

```text
~/Library/Application Support/YuE2Studio/
```

作品默认保存在：

```text
~/Music/YuE2Studio/
```

路径中不会写死开发者姓名；换到另一台 Mac 会自动使用对方账户的用户目录。

## 4. 基本使用

### 4.1 从音频提取 ABC

1. 在 Source 区域拖入音频或点击“选择文件”。
2. 选择“主唱 + 器乐主旋律”“只要主唱旋律”或“旋律 + 和弦”。
3. 点击“转写成 ABC”。界面会显示累计耗时，完成后显示相对实时速度。
4. 转写结果会自动填入 ABC 区域，可复制、编辑并用于后续生成。
5. 点击停止可中断转写，后台进程会被终止并释放 MPS 内存。

### 4.2 生成音乐

1. 填写风格提示词和歌词。
2. 按需要选择 Planning、质量、歌曲数量、最大长度与随机种子。
3. 默认选择 MLX；PyTorch（MPS）仅作为兼容路径，Neural Engine 适合特定长度。
4. 如需参考旋律，可使用转写生成的 ABC。
5. 点击 Generate。点击停止可取消排队或正在执行的生成任务。

提示：完整质量通常明显慢于草稿；首次生成还包含模型加载时间。

## 5. 下载、自检与修复机制

- 优先测试可用的国内 PyPI、Python 与模型镜像，并按测速结果选择来源；
- 大模型采用多连接、分段、断点续传；临时分片不会被当成完整权重；
- YuE2-3B 与 YuE2-Vae 分别按最终文件及精确字节数校验；
- 主模型完整而 VAE 缺失时，仅续传 VAE，不重复下载主模型；
- 安装器每次启动都会重写当前用户、当前 App 位置对应的引擎配置；
- 已安装权重通过本地绝对路径交给生成引擎，不依赖联网解析 Hugging Face revision；
- App 不会携带开发机器的用户名、工程路径或 `engines.json`。

镜像是外部服务，速度与可用性无法永久保证。所有镜像不可用时，安装器可能回退到上游来源；用户应遵守所用镜像及上游服务条款。

## 6. 常见问题

### 安装提示模型不完整

这是自检在阻止不完整权重进入生成流程。保持 App 开启并继续安装即可；已有分片会复用。

### 生成提示缺少 `model.safetensors`

说明对应权重尚未合并完成。1.2.4 不会再把这种状态判定为安装完成；重启 App 并完成补齐。

### 输入框不能粘贴

1.2.1 起已提供标准 macOS 编辑菜单，支持 ⌘V、⌘C、⌘X、⌘A、⌘Z 与 ⇧⌘Z。

### 窗口不好拖动

1.2.2 起顶部磨砂区域是原生拖动区，并避开红黄绿按钮。

### 如何彻底卸载

从应用菜单选择“干净卸载…”。App、本地 Python 环境、模型、缓存、上传与日志会移到废纸篓；已经生成到音乐目录的作品不会删除。

## 7. 隐私与网络说明

- 音频、歌词、ABC 和生成结果在本机处理；界面只连接 `127.0.0.1` 本地服务；
- 未集成账户、广告、分析或遥测；运行环境会显式关闭 Hugging Face 遥测；
- 首次安装及修复会联网探测镜像并下载 Python、依赖、模型与必要文件；外部下载服务会看到网络请求及 IP 地址；
- 导入的音频会复制到本地应用数据目录，日志也保存在本地；
- 用户应只处理自己拥有权利或已获授权的音频、歌词、旋律与其他素材。

## 8. 许可证与合规

### 8.1 核心许可

| 组件 | 许可概要 |
|---|---|
| YuE2 代码 | Apache License 2.0 |
| YuE2-3B / YuE2-Vae 权重 | CC BY-NC 4.0 |
| MERT-v2-FullSong 权重 | CC BY-NC 4.0 |
| SheetSage2 权重 | 包内模型许可与第三方声明；按非商业用途保守处理 |
| uv | Apache-2.0 / MIT（以上游发布为准） |
| 其他依赖 | 各自上游许可证 |

模型与代码的完整许可证、NOTICE 和第三方声明保存在 App 内：

```text
YuE2 Studio.app/Contents/Resources/payload/
```

主要来源与署名：

- YuE2-3B / YuE2-Vae：<https://huggingface.co/m-a-p/YuE2-3B>
- MERT-v2-FullSong：<https://huggingface.co/m-a-p/MERT-v2-FullSong>
- SheetSage2：<https://huggingface.co/m-a-p/SheetSage2>
- YuE2 代码：<https://github.com/multimodal-art-projection/YuE>
- YuE Studio 参考项目：<https://github.com/tonywestonuk/YuE-Studio>

CC BY-NC 4.0 要求适当署名、提供许可链接、标明修改，并限制商业用途。官方摘要与法律文本见：<https://creativecommons.org/licenses/by-nc/4.0/>。

### 8.2 非商业限制

本软件会下载并使用采用 CC BY-NC 4.0 的模型权重，因此本发布包应定位为个人、教育、研究及其他非商业用途。不要把它直接销售、作为收费服务提供、嵌入商业产品，或在未取得额外授权时用于以商业利益为主要目的的场景。

模型许可是否以及如何影响具体生成结果，需要结合许可文本、司法辖区、使用方式和输出内容判断；本文不作确定法律结论。计划商业使用时，应向权利人取得许可并咨询专业法律人士。

### 8.3 音频、歌词与翻唱

转写或生成工具不会改变输入材料的权利归属。公开发布翻唱、改编、歌词、采样或高度相似旋律，可能涉及词曲、录音、表演、人格权或平台规则。用户负责取得必要授权并审核输出。

### 8.4 无担保

模型及多项依赖按“现状”提供。AI 输出可能不准确、不可用、与现有作品相似或包含不适合发布的内容；用户应人工审核并自行承担使用风险。

## 9. 发布状态与签名

当前构建可以作为本机测试版或小范围非商业测试候选版，但**尚不能称为面向公众的正式 macOS 发布版**，原因是：

1. 当前仅为 ad hoc 签名，没有 Apple Developer ID；
2. 尚未提交 Apple 公证（notarization）并装订票据；
3. 当前只有 arm64 架构，不支持 Intel Mac；
4. 在一台全新、受支持的 Mac 上完成首次安装与一次完整生成的最终验收仍应执行；
5. CC BY-NC 4.0 模型权重决定了该版本应按非商业发布处理。

Apple 建议在 Mac App Store 外分发的应用使用 Developer ID 签名并提交公证，以便 Gatekeeper 验证来源和完整性：

- <https://developer.apple.com/developer-id/>
- <https://developer.apple.com/documentation/security/notarizing-macos-software-before-distribution>

### 正式发布前检查表

- [ ] 使用 `Developer ID Application` 证书及 Hardened Runtime 重新签名；
- [ ] 将发布容器提交 Apple 公证，并把票据 staple 到分发文件；
- [ ] 在启用 Gatekeeper 的干净 Apple Silicon Mac 上测试首次启动；
- [ ] 完成模型安装、音频转写、MLX 生成、中断、重启续传、自检修复和干净卸载；
- [ ] 确认许可证、署名、隐私说明与下载来源随包提供；
- [ ] 若用于商业场景，先取得模型权利人的商业许可；
- [ ] 为用户提供版本校验值、支持渠道和安全更新方式。

## 10. 本版本已修复并静态验证的事项

- 日志区域固定高度并自动滚动；
- 文件选择按钮与拖放均可用；
- 输入框标准复制、粘贴与撤销快捷键；
- 高对比选中状态、固定磨砂顶栏、原生窗口拖动区；
- 转写耗时与实时速度显示；
- 转写、生成均可中断；
- 当前用户路径动态生成，不包含开发者个人路径；
- YuE2 本地模型路径直连，缓存 revision 自动修复；
- 3B 与 VAE 分别精确校验，残留分片不会误报完成；
- 缺 VAE 时仅续传 VAE；
- 构建排除 `__pycache__` / `.pyc`，运行期禁止向签名包写入字节码；
- 干净卸载入口可用；
- JavaScript、Python、构建脚本及代码签名结构已完成静态检查。

上述清单不等于已完成 Apple 公证，也不替代全新机器端到端验收。
